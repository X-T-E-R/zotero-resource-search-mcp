# Skill file location

The canonical Agent Skill document for this plugin is:

**[`docs/skills/SKILL.md`](../docs/skills/SKILL.md)**

Use **Zotero → Settings → Resource Search MCP → Agent Skills** to export or install `SKILL.md` to Cursor, Claude Code, or Codex with the correct MCP port baked in.
